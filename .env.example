# Gmail認証情報
# GmailアカウントのメールアドレスとAppパスワードを設定してください
# Appパスワードの取得方法: https://support.google.com/accounts/answer/185833

GMAIL_EMAIL=your-email@gmail.com
GMAIL_PASSWORD=your-app-password

# 楽天アカウント認証情報
# 楽天リンクをクリックした際にログインが必要な場合に使用
RAKUTEN_USER_ID=your-rakuten-user-id
RAKUTEN_PASSWORD=your-rakuten-password

# 検索条件
# Gmailで楽天メールを検索するためのクエリ
# in:inbox - 受信トレイのみを対象
# -in:trash - ゴミ箱を除外
# -in:spam - 迷惑メールを除外
SEARCH_QUERY=from:rakuten in:inbox -in:trash -in:spam

# ブラウザ設定
# headlessモードで動作させるか (true/false)
HEADLESS=false

# タイムアウト設定（ミリ秒）
TIMEOUT=30000

# 画像認識設定
# 画像マッチングの閾値 (0.0～1.0)
# 値が高いほど厳密にマッチング、低いほど緩くマッチング
# 推奨値: 0.75～0.85（デフォルト: 0.8）
IMAGE_MATCH_THRESHOLD=0.8

# ピクセル単位のマッチング閾値 (0.0～1.0)
# 各ピクセルの色差をどの程度許容するか
# 値が高いほど色の違いを許容、低いほど厳密に比較
# 推奨値: 0.1～0.2（デフォルト: 0.1）
PIXEL_MATCH_THRESHOLD=0.1

# 検知対象画像のディレクトリパス
IMAGES_DIR=./images
